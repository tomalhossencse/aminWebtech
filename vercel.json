{
    "version": 2,
    "builds": [
        {
            "src": "server-complete.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/server-complete.js"
        }
    ],
    "env": {
        "NODE_ENV": "production"
    },
    "functions": {
        "server-complete.js": {
            "maxDuration": 30
        }
    }
}